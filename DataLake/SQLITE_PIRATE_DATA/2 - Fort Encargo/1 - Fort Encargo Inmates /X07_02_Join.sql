SELECT * FROM(
SELECT 
    NAME,
    DESCRIPTION
FROM(
SELECT 
   NAME,
   CRIME
FROM FREEDINMATES 

UNION

SELECT 
    NAME,
    CRIME
FROM RUMTHEFT) INNER JOIN CRIME
ON CRIME = CRIME.CRIME_ID
GROUP BY NAME
HAVING DESCRIPTION = 'Theft')